# TERANGASCHOOL - Variables d'Environnement
# Copiez ce fichier vers .env et remplissez les valeurs

# === OBLIGATOIRE ===

# URL de connexion à la base de données PostgreSQL
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://terangaschool_user:password@localhost:5432/terangaschool

# Clé secrète pour les sessions (générez avec: openssl rand -hex 32)
SESSION_SECRET=your-secret-key-here-at-least-32-characters

# === OPTIONNEL ===

# Port du serveur (défaut: 5000, Render utilise 10000)
PORT=5000

# Environnement (development | production)
NODE_ENV=development

# Variables PostgreSQL (si non utilisation de DATABASE_URL)
PGHOST=localhost
PGPORT=5432
PGUSER=terangaschool_user
PGPASSWORD=your-password
PGDATABASE=terangaschool
